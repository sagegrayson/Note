<section class="container">
  <h3 class="">{{post.genre.name}}</h3>
  <section class="flex-row justify-center">

 <section class="card">
  <header>{{post.user.username}}</header>
  <div class="card-body">
    <p>{{post.post_text}}</p>
  </div>
 </section>
{{!-- </section>
    {{#each post}}
    {{> post-details}}
    {{/each}}
  </section> --}}
</section>